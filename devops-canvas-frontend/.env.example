# Optional. Leave unset to use /api on the same host as the page (Docker UI + nginx, or Vite dev proxy).
# VITE_API_URL=http://localhost:8080/api
# Optional. Leave unset to derive WebSocket URL from the page (e.g. ws://localhost:3000/api/ws in dev).
# VITE_WS_URL=ws://localhost:8080/api/ws
